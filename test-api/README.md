### Important note


For runpod serverless dockerfile is needed.
As those handlers need large huggingface models, the default configuration is not enough,
need to think about dynamic volume mounting or other solutions to handle large models.

